// websocket actions

syntax = "proto3";

package messenger.actions.ws.messages;

// action login and signup have similar inputs and results
message ActionAuth {
  string username = 1;
  string password = 2;
}

message ActionAuthResult {
  string token = 1;
}
